<div class="page-header">
  <h1><%= link_to @post.name, @post %></h1>
</div>
<p><%= @post.text %></p>

<h2>Comments</h2>
<%= commentator(
  url: post_comments_path(@post),
  comments: @post.comments_json(self),
  comment_template: "comments/comment",
  reply_template: "comments/reply"
) %>
